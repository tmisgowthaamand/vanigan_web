# Same-origin proxy path. Vercel (vercel.json) and Vite dev (vite.config.js)
# both forward /proxy-api/* to the Render backend, which avoids CORS on any
# Vercel preview/production domain.
VITE_API_BASE_URL=/proxy-api
